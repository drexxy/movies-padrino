<h1><%= @movie.title %> - Reviews </h1>

<% @review.each do |review| %>
<p><%= review.name %> - <%= review.rating %>/10</p>
<%= link_to "Read the full review!", url_for(:reviews, :index, :id => review.id, :movie_id => params['movie_id']) %>
<% end %>