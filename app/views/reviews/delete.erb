<h1><%= @review.name %>'s review of <%= @movie.title %></h1>

<dl>
<dt>Rating</dt>
<dd><%= @review.rating %></dd>
</dl>

<p><%= @review.review %>

<p>Are you sure you want to delete this well nuanced critique?</p>

<% form_for @movie, url_for(:reviews, :delete, :id => params['id'], :movie_id => params['movie_id']), method: :delete do |f| %>

<%= f.submit "Delete Review" %>

<% end %>